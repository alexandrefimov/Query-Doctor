# Keep impala-shell isolated from Query Doctor's main runtime. The bootstrap
# script installs impala_shell itself with --no-deps, then installs these
# runtime dependencies so security-patched transitive pins can move without
# replacing the legacy shell package used by the CDH6 smoke path.
bitarray==2.3.0
configparser==4.0.2
kerberos==1.3.1
prettytable==0.7.2
pure-sasl==0.6.2
sasl==0.3.1
six==1.14.0
sqlparse==0.6.0
thrift==0.16.0
thrift-sasl==0.4.3
